apiVersion: apps/v1
kind: Deployment
metadata:
  name: 101-scheduler-kube
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scheduler
  template:
    metadata:
      labels:
        app: scheduler
    spec:
      containers:
        - name: scheduler
          image: elygo/101-scheduler-kube
